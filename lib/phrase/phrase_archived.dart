import 'package:flutter/material.dart';

class PhraseArchived extends StatelessWidget {
  const PhraseArchived({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Frases arquivadas'),
      ),
    );
  }
}
