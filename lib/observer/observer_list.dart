import 'package:flutter/material.dart';

class ObserverList extends StatelessWidget {
  const ObserverList({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Observando frases'),
      ),
    );
  }
}
